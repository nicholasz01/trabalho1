const botao = document.getElementById('btnSimular');
(adventListener)
botao.addEventListener('click',funtion({
    input(getElemetById
        const metodoSelecionado = document.getElementById()
    )
}))